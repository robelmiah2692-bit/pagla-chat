name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      - name: Prepare Android Signing
        run: |
          # যদি কি-স্টোর না থাকে তবেই কেবল একবার তৈরি করবে (ফিক্সড চাবি)
          mkdir -p android/app
          if [ ! -f android/app/upload-keystore.jks ]; then
            keytool -genkey -v -keystore android/app/upload-keystore.jks \
            -storepass "pagla123" -alias "pagla_alias" -keypass "pagla123" \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Pagla, O=Chat, C=BD"
          fi

      - name: 🔑 SHA কোড সরাসরি দেখা (এটি ফায়ারবেসে একবারই বসাবেন)
        run: |
          cd android
          # কি-স্টোর থেকে SHA-1 বের করা
          keytool -list -v -keystore app/upload-keystore.jks -alias "pagla_alias" -storepass "pagla123" > sha_report.txt
          echo "**************************************************"
          echo "এই SHA-1 টি ফায়ারবেসে চিরস্থায়ীভাবে বসান:"
          grep "SHA1:" sha_report.txt
          echo "**************************************************"

      - name: Build APK
        run: |
          # সরাসরি রিলিজ বিল্ড (সব ফিচার সহ)
          flutter build apk --release --target=lib/main.dart

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PaglaChat-Fixed-SHA-APK
          path: build/app/outputs/flutter-apk/app-release.apk
