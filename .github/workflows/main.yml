name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          cache: true

      - name: Reconstruct and Build
        run: |
          # ১. প্রজেক্টের নাম ফিক্স করা
          sed -i 's/name: .*/name: pagla_chat/g' pubspec.yaml
          
          # ২. অ্যান্ড্রয়েড ফোল্ডার নতুন করে তৈরি করা
          rm -rf android
          flutter create . --project-name pagla_chat --platforms android
          
          # ৩. প্যাকেজ গেট করা
          flutter pub get
          
          # ৪. সেটিংস ফিক্স (minSdkVersion 21)
          sed -i 's/minSdkVersion .*/minSdkVersion 21/g' android/app/build.gradle
          
          # ৫. পারমিশন দেওয়া
          chmod +x android/gradlew

      - name: 🔑 🕵️‍♂️ আসল SHA-1 এবং SHA-256 খুঁজে বের করো
        run: |
          echo "===================================================="
          echo "নিচে আপনার আসল SHA কী-গুলো দেওয়া হলো:"
          echo "===================================================="
          cd android && ./gradlew signingReport | grep -A 10 "Variant: debug" | grep -E "SHA1|SHA-256"
          echo "===================================================="

      - name: Build APK
        run: |
          flutter build apk --release --target=lib/main.dart

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PaglaChat-Success-APK
          path: build/app/outputs/flutter-apk/app-release.apk
