name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          cache: true

      - name: Reconstruct and Build
        run: |
          sed -i 's/name: .*/name: pagla_chat/g' pubspec.yaml
          rm -rf android
          flutter create . --project-name pagla_chat --platforms android
          flutter pub get
          sed -i 's/minSdkVersion .*/minSdkVersion 21/g' android/app/build.gradle
          chmod +x android/gradlew

      - name: 🔑 SHA কোড সরাসরি দেখা
        run: |
          # ফোল্ডার তৈরি এবং কী-জেনারেট করা (ফিক্সড)
          mkdir -p ~/.android
          keytool -genkey -v -keystore ~/.android/debug.keystore -storepass android -alias AndroidDebugKey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          
          cd android
          ./gradlew signingReport > report.txt
          echo "**************************************************"
          echo "নিচের কোডগুলো কপি করে ফায়ারবেসে বসান:"
          echo ""
          grep "SHA1" report.txt | head -n 1
          grep "SHA-256" report.txt | head -n 1
          echo "**************************************************"

      - name: Build APK
        run: |
          flutter build apk --release --target=lib/main.dart

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PaglaChat-Success-APK
          path: build/app/outputs/flutter-apk/app-release.apk
