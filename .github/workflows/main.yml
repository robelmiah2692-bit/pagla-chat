name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          cache: true

      # --- মোবাইল ইউজারদের জন্য স্পেশাল ফাইল জেনারেটর ---
      - name: Generate Missing Android Files
        run: |
          cd android
          # যদি গ্র্যাডল ফাইল না থাকে, তবে এটি জেনারেট করবে
          if [ ! -f gradlew ]; then
            # গিটহাবের ভেতরেই ফাইলটা বানানোর কমান্ড
            gradle wrapper --gradle-version 8.0
          fi
          chmod +x gradlew

      - name: Install Dependencies
        run: |
          flutter pub get
          flutter clean

      - name: Prepare Android Signing
        run: |
          mkdir -p android/app
          if [ ! -f android/app/upload-keystore.jks ]; then
            keytool -genkey -v -keystore android/app/upload-keystore.jks \
            -storepass "pagla123" -alias "pagla_alias" -keypass "pagla123" \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Pagla, O=Chat, C=BD"
          fi

      - name: 🔑 SHA কোড সরাসরি দেখা
        run: |
          cd android
          keytool -list -v -keystore app/upload-keystore.jks -alias "pagla_alias" -storepass "pagla123" > sha_report.txt
          echo "SHA1 কোড নিচে দেওয়া হলো:"
          grep "SHA1:" sha_report.txt

      - name: Build APK
        run: |
          # আপনার ১৫ সিট এবং ইউটিউব ফিচার সহ রিলিজ বিল্ড
          flutter build apk --release --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PaglaChat-Fixed-SHA-APK
          path: build/app/outputs/flutter-apk/app-release.apk
