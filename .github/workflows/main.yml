name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          cache: true

      - name: 🔥 Fix Android Config (মোবাইল ইউজারদের জন্য ফিক্স)
        run: |
          cd android
          # গ্র্যাডল ৮.২ সেট করা
          gradle wrapper --gradle-version 8.2 --distribution-type all
          
          # সবচেয়ে জরুরি: ফ্লাটার এসডিকে পাথ ম্যানুয়ালি লিখে দেওয়া
          echo "sdk.dir=$ANDROID_SDK_ROOT" > local.properties
          echo "flutter.sdk=$FLUTTER_HOME" >> local.properties
          
          # settings.gradle ঠিক করা
          echo "include ':app'" > settings.gradle
          
          chmod +x gradlew

      - name: Install Dependencies
        run: |
          flutter pub get
          flutter clean

      - name: Prepare Android Signing
        run: |
          mkdir -p android/app
          if [ ! -f android/app/upload-keystore.jks ]; then
            keytool -genkey -v -keystore android/app/upload-keystore.jks \
            -storepass "pagla123" -alias "pagla_alias" -keypass "pagla123" \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Pagla, O=Chat, C=BD"
          fi

      - name: 🔑 SHA কোড চিরস্থায়ীভাবে দেখা
        run: |
          cd android
          keytool -list -v -keystore app/upload-keystore.jks -alias "pagla_alias" -storepass "pagla123" > sha_report.txt
          echo "**************************************************"
          echo "এই SHA-1 টি ফায়ারবেসে চিরস্থায়ীভাবে বসান:"
          grep "SHA1:" sha_report.txt
          echo "**************************************************"

      - name: Build APK
        run: |
          # রিলিজ বিল্ড শুরু
          flutter build apk --release --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PaglaChat-Fixed-APK
          path: build/app/outputs/flutter-apk/app-release.apk
