name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          cache: true

      - name: 🛠️ Reconstruct (Based on Successful Run #170)
        run: |
          # ১. প্রজেক্টের নাম ফিক্স করা
          sed -i 's/name: .*/name: pagla_chat/g' pubspec.yaml
          
          # ২. অ্যান্ড্রয়েড ফোল্ডার নতুন করে তৈরি (সফল ১৭০ এর লজিক)
          # এটি করলে সব গ্র্যাডল জট এক নিমেষে খুলে যাবে
          rm -rf android
          flutter create . --project-name pagla_chat --platforms android
          
          # ৩. সব প্লাগইন ইন্সটল (Agora, YouTube, Firebase)
          flutter pub get
          
          # ৪. অ্যান্ড্রয়েড লেভেলে ফিচারের জন্য সেটিংস আপডেট
          # minSdkVersion ২১ না করলে ইউটিউব বা আগোরা চলবে না
          sed -i 's/minSdkVersion .*/minSdkVersion 21/g' android/app/build.gradle
          
          # ৫. লোকাল পাথ এবং পারমিশন ঠিক করা
          echo "sdk.dir=$ANDROID_HOME" > android/local.properties
          echo "flutter.sdk=/opt/hostedtoolcache/flutter/stable-3.19.0-x64" >> android/local.properties
          chmod +x android/gradlew

      - name: 🔑 Key Store & SHA (Firebase Ready)
        run: |
          # আপনার দেওয়া পাসওয়ার্ড দিয়ে কি-স্টোর তৈরি
          mkdir -p android/app
          keytool -genkey -v -keystore android/app/upload-keystore.jks \
            -storepass "pagla123" -alias "pagla_alias" -keypass "pagla123" \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Pagla, O=Chat, C=BD" || true
            
          cd android
          ./gradlew signingReport | grep -E "SHA1|SHA-256" || true

      - name: Build APK (Release)
        run: |
          # আপনার সব ফিচার অক্ষুণ্ণ রেখে বিল্ড শুরু
          flutter build apk --release --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PaglaChat-v204-Success
          path: build/app/outputs/flutter-apk/app-release.apk
